#include<stdio.h>

int main()
{
    int t,n,a,m;
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d",&n);//cout<<f<<setprecision(5)[5.2451] 
        while(n!=0)
        {
            a=n%10;
            switch (a)
            {
            case 1

              
                break;
            
            default:
                break;
            }
        }
    }
}